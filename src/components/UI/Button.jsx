import React from 'react';

const Button = (props) => {
    return (
        <button className="button">
            {/*что-то потом сюда впишем может*/}
        </button>
    );
};

export default Button;